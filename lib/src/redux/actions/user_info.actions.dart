import 'package:multi_camera_photography_fe/src/models/user_info.dart';

// Temporary code, need to change
class GetUserInfoAction {
  UserInfo userInfo;
  GetUserInfoAction({required this.userInfo});
}
